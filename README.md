# CRM-SlspplnBack

### Setup

The package.json file has a couple scripts that will enable you to run the API and add changes, with the changes being applied automatically to the `dist/app.js` file.

```
npm install
npm run serve
```
